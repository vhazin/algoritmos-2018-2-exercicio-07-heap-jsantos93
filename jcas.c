#include <stdio.h>

int main()
{
    int n;
    char heap;

    scanf("%d", &n);

    for (int *i = 0; i < (n - 1); i++)
    {
        scanf(" %c", &heap[i]);
    }

    printf("fon");

    return 0;
}